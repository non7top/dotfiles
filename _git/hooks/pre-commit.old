#!/usr/bin/env bash
# File generated by pre-commit: https://pre-commit.com
# ID: 138fd403232d2ddd5efb44317e38bf03

#ARGS=(hook-impl --config=~/.git/hooks/.pre-commit-config.yaml --hook-type=pre-commit)

#HERE="$(cd "$(dirname "$0")" && pwd)"
#ARGS+=(--hook-dir "$HERE" -- "$@")

if command -v pre-commit > /dev/null; then
    #exec pre-commit "${ARGS[@]}"
    pre-commit run --config ~/.git/.pre-commit-config.yaml --all-files
else
    echo "Error, be sure to install pre-commit"
    exit 1
fi
